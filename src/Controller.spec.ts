class SaveMoneyController {
    save(arg0: number) {
       
    }
}

describe('Controller', () => {

    it('should save money ok', () => {

        new SaveMoneyController().save(1000);

    });

});
